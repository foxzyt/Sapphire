name: Release

on:
  push:
    tags:
      # Only real version tags (v1.0.9, v2.0, ...). Deliberately does NOT match
      # draft tags like v-draft-*/v-next-*, which must never publish a release.
      - 'v[0-9]*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest
    env:
      # Pin the ccache dir to the workspace so the msys2 shell (native ccache
      # binary) and actions/cache (host runner) agree on the same location.
      CCACHE_DIR: ${{ github.workspace }}\.ccache

    steps:
      # workflow_dispatch can be run from any branch; without a real v* tag
      # checked out, the release would be tagged with the branch name (e.g.
      # "main"). Fail fast instead.
      - name: Validate ref is a release tag
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        run: |
          if [ "${{ github.ref_type }}" != "tag" ] || [[ "${{ github.ref_name }}" != v[0-9]* ]]; then
            echo "::error::This workflow must be dispatched from a version tag (v1.2.3), not '${{ github.ref_name }}'. Push a tag and run it from there."
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup MSYS2 (MINGW64)
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          install: >-
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-make
            mingw-w64-x86_64-sfml
            mingw-w64-x86_64-openssl
            mingw-w64-x86_64-ccache

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: .ccache
          key: ${{ runner.os }}-ccache-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-ccache-

      - name: Configure CMake
        shell: msys2 {0}
        run: |
          cmake -S . -B build -G "MinGW Makefiles" \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DSFML_DIR=/mingw64/lib/cmake/SFML \
            -DSFML_STATIC_LIBRARIES=ON \
            -DOPENSSL_ROOT_DIR=/mingw64 \
            -DOPENSSL_USE_STATIC_LIBS=TRUE

      - name: Build
        shell: msys2 {0}
        run: cmake --build build --config Release --parallel

      - name: Package artifacts
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path release_artifacts | Out-Null
          if (Test-Path build/sapphire.exe) { Copy-Item build/sapphire.exe release_artifacts/ }
          if (Test-Path build/runner.exe) { Copy-Item build/runner.exe release_artifacts/ }
          if (Test-Path build/mine.exe) { Copy-Item build/mine.exe release_artifacts/ }
          if (Test-Path build/spack.exe) { Copy-Item build/spack.exe release_artifacts/ }
          if (Test-Path README.md) { Copy-Item README.md release_artifacts/ }
          Compress-Archive -Path release_artifacts/* -DestinationPath sapphire-windows.zip -Force

      - name: Get previous tag
        id: prev_tag
        shell: bash
        run: |
          CUR_TAG="${{ github.ref_name }}"
          # Get all semantic tags, sort them, find the one right before CUR_TAG
          PREV_TAG=$(git tag -l "v[0-9]*" | sort -V | grep -B 1 "^${CUR_TAG}$" | head -n 1)
          if [ "$PREV_TAG" == "$CUR_TAG" ]; then
            PREV_TAG=""
          fi
          echo "PREV_TAG=${PREV_TAG}" >> "$GITHUB_ENV"
          echo "Calculated PREV_TAG: $PREV_TAG"

      - name: Run Copilot Release Notes
        id: copilot_release
        uses: github/copilot-release-notes@v1
        continue-on-error: true
        with:
          base-ref: ${{ env.PREV_TAG || 'main' }}
          head-ref: ${{ github.ref_name }}
          instructions: ".github/release-notes-instructions.md"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_TOKEN }}

      - name: Prepare release body
        shell: bash
        env:
          MARKDOWN: ${{ steps.copilot_release.outputs.release-notes }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          if [ -n "$MARKDOWN" ]; then
            echo "$MARKDOWN" > release_body.md
          else
            echo "Copilot failed or returned empty. Falling back to native GitHub release notes..."
            if [ -n "$PREV_TAG" ]; then
              gh api --method POST -H "Accept: application/vnd.github+json" \
                repos/${{ github.repository }}/releases/generate-notes \
                -f tag_name="${{ github.ref_name }}" \
                -f previous_tag_name="${PREV_TAG}" -q .body > release_body.md
            else
              gh api --method POST -H "Accept: application/vnd.github+json" \
                repos/${{ github.repository }}/releases/generate-notes \
                -f tag_name="${{ github.ref_name }}" -q .body > release_body.md
            fi
          fi

          echo "" >> release_body.md
          cat .github/release-examples.md >> release_body.md

          cat .github/release-footer.md >> release_body.md

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: sapphire-windows.zip
          tag_name: ${{ github.ref_name }}
          name: Sapphire ${{ github.ref_name }}
          body_path: release_body.md
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
