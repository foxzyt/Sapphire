function main() {
    var m = {"name": "Sapphire", "version": 1.8};
    m["author"] = "User";
    
    if (m["name"] == "Sapphire") {
        if (m["version"] == 1.8) {
            print("Map test passed.");
        }
    }
}
main();
